using KMeansClustering.Data;
using System;

namespace KMeans_Clustering
{
    class Program
    {
        static void Main(string[] args)
        {
            for(int i = 0; i < 100; i++)
            {
                var obs = new FileReader().GetObservations;
            }

            Console.Read();
        }
    }
}
